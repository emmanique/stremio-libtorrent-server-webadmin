# Branching model notice

The previous `develop/2.x` / per-version branching model is retired.

The active model is documented in [BRANCHING.md](BRANCHING.md): only `main` and `development` are long-lived; `feature/*`, `fix/*`, `release/*` and `hotfix/*` are temporary.
