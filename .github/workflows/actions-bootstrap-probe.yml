name: Actions bootstrap probe

on:
  push:
    branches: [development]

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions job graph started successfully"
